package chapter04;
import java.util.Date;
public class Java10_Import {
    public static void main(String[] args) {
        /*
        TODO 面向对象 - import
            java.lang里的包可以不写java.lang，当前包也不需要写，但是其他包需要写
            在package后面和类的前面执行
            主要用于在使用类前准备好的包
            关键字可以多次使用，导入多个类
            如果同一个包中需要导入大量的类，那么可以使用通配符*来简化操作
            如果import了不同包中相同名称的类，那么还是需要在使用时增加包名
         */

    }
}
